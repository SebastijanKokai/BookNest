import 'package:flutter/material.dart';

class ReadingLogPage extends StatelessWidget {
  const ReadingLogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Reading log page."),
    );
  }
}
